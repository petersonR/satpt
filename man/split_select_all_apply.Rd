% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split-select-all-apply.R
\name{split_select_all_apply}
\alias{split_select_all_apply}
\title{Split a select-all-that-apply response column into binary indicators}
\usage{
split_select_all_apply(x, sep = "|", values = NULL)
}
\arguments{
\item{x}{Character vector (or factor) of separator-delimited responses.}

\item{sep}{Separator string. Default \code{"|"}, the convention used by the
bundled \link{diagnoses} data set. Matched literally (not as a regular
expression).}

\item{values}{Optional character vector of tokens to use as the column
set. When \code{NULL} (the default), the unique non-empty tokens observed in
\code{x} are used and sorted alphabetically.}
}
\value{
A \code{data.frame} with one column per distinct token in \code{x} (or per
element of \code{values}, when supplied). All columns are character vectors
with values \code{"Yes"}, \code{"No"}, or \code{NA}.
}
\description{
Converts a character vector whose entries hold one or more
response tokens separated by \code{sep} (e.g. \code{"Broadrange|MNGS|Other"}) into a
\code{data.frame} with one column per token, each entry \code{"Yes"} or \code{"No"}
depending on whether the token was selected. NA entries (and entries that
contain only whitespace or empty tokens) become a row of \code{NA}s in the
output.
}
\details{
This is the explicit, exported version of the boilerplate shown
in the \emph{select-all-apply} vignette. It produces a \code{data.frame} shaped
exactly the way \code{\link[=satpt]{satpt()}} expects for select-all-that-apply
analysis: one indicator column per response item.

Pass the result directly to \code{\link[=satpt]{satpt()}} (with \code{select_all_apply = TRUE}) or include it as a question column inside a survey passed to
\code{\link[=satpt_survey]{satpt_survey()}}.
}
\examples{
data(diagnoses)
q1 <- satpt::split_select_all_apply(x = diagnoses$q1, sep = "|")
head(q1)

# Feed straight into satpt():
satpt::satpt(
  y = q1, by = diagnoses$wave, select_all_apply = TRUE
)

}
