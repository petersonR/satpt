% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/satpt-survey.R
\name{satpt_survey}
\alias{satpt_survey}
\alias{print.satpt_survey}
\title{Saturation point analysis across an entire survey}
\usage{
satpt_survey(data, by = NULL, questions = NULL, split = NULL, ...)

\method{print}{satpt_survey}(x, ...)
}
\arguments{
\item{data}{A \code{data.frame} (or named \code{list}) of survey responses with
one column per question. Columns may be \code{vector}s, \code{factor}s, or nested
\code{data.frame}s/\code{matrix} objects (the latter are treated as
select-all-that-apply questions).}

\item{by}{Either the name of a column in \code{data} whose values indicate
the data collection wave for each row, \strong{or} an external vector of
wave indicators with one entry per row of \code{data}. \code{NULL} (the default)
suppresses the test for response bias and the heterogeneity index, the
same way \code{\link[=satpt]{satpt()}} behaves when \code{by} is omitted.}

\item{questions}{Optional character vector of column names from \code{data}
to analyze. Default analyzes every column other than the column named
in \code{by} (when \code{by} is a column name).}

\item{split}{Optional named list (or named character vector) of
separators for select-all-that-apply questions encoded as a single
delimited string per response. For example,
\code{split = list(q1 = "|")} tells \code{satpt_survey()} to run
\code{\link[=split_select_all_apply]{split_select_all_apply()}} on \code{data$q1} with \code{sep = "|"} before
the analysis, so each unique response token becomes its own indicator
column. Names not present in \code{data} are ignored with a warning.}

\item{...}{Additional arguments forwarded to \code{\link[=satpt]{satpt()}} for every
question (e.g. \code{threshold}, \code{alpha}, \code{exclude}).}

\item{x}{A \code{satpt_survey} object to be printed.}
}
\value{
An object with \code{S3} class \code{"satpt_survey"} containing:
\describe{
\item{\code{summary}}{A \code{data.frame} with one row per question and the
columns \code{question}, \code{n} (responses observed), \code{max_se} (largest
standard error driving the saturation decision), \code{saturation}
(logical), and \code{n_to_saturation} (projected additional responses
needed; \code{0} when already saturated).}
\item{\code{results}}{A named \code{list} of \code{satpt} objects, one per question,
in the same order as \code{summary$question}.}
}
}
\description{
Run \code{\link[=satpt]{satpt()}} on every question in a survey and
return a tidy summary that highlights which questions have reached
saturation, which haven't, and how many additional responses each
un-saturated question is projected to need.
}
\details{
\code{satpt_survey()} is a convenience wrapper for the common
workflow of "run satpt on each question of my survey and tell me which
ones are still collecting data." Multi-column inputs (nested
\code{data.frame}/\code{matrix} columns, typical of select-all-that-apply
questions) are forwarded to \code{satpt()} with \code{select_all_apply = TRUE} so
you don't see the wide-\code{y} warning.

The returned object is an \code{S3} object of class \code{"satpt_survey"} with
two top-level slots: \code{summary} (a tidy \code{data.frame}, one row per
question) and \code{results} (a named \code{list} of the underlying \code{satpt}
objects, available for drill-down).
}
\examples{
data(diagnoses)

# Analyze every question in the diagnoses survey, using `wave` to
# detect response bias across data collection periods. q1 is a
# select-all-that-apply question encoded as pipe-delimited strings,
# so `split` is used to convert it before analysis.
satpt_survey(diagnoses, by = "wave", split = list(q1 = "|"))

# Analyze a subset of questions; drill down into one.
res <- satpt_survey(diagnoses, by = "wave", questions = "q2")
res$results$q2

}
\seealso{
\code{\link[=satpt]{satpt()}}
}
