% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/char_matrix.R
\name{char_matrix}
\alias{char_matrix}
\alias{char_matrix.default}
\alias{char_matrix.factor}
\alias{char_matrix.list}
\alias{char_matrix.data.frame}
\alias{char_matrix.data.table}
\alias{char_matrix.matrix}
\title{Coerce Input to Character Matrix with Meaningful Column Names}
\usage{
char_matrix(y, cname = NULL)

\method{char_matrix}{default}(y, cname = NULL)

\method{char_matrix}{factor}(y, cname = NULL)

\method{char_matrix}{list}(y, cname = NULL)

\method{char_matrix}{data.frame}(y, cname = NULL)

\method{char_matrix}{data.table}(y, cname = NULL)

\method{char_matrix}{matrix}(y, cname = NULL)
}
\arguments{
\item{y}{An object to be converted to a character matrix. Acceptable types
include \code{vector}, \code{factor}, \code{matrix}, \code{data.frame}, \code{data.table}, \code{tibble},
or \code{list}.}

\item{cname}{A character vector of column names whose length must match the
number of output columns. Default is \code{NULL}, which means \code{factor}s and
\code{atomic} vectors will not be assigned a column name and \code{list}s,
\code{data.frame}s, and \code{matrix} inputs will fall back to their existing column
names (or synthesized \code{V1}, \code{V2}, ... names for unnamed lists).}
}
\value{
A \code{character} matrix with meaningful column names when possible.
}
\description{
Converts various \emph{R} object types, such as vectors, factors, data frames,
data tables, tibbles, and lists, into a character matrix, preserving or
assigning column names intelligently.
}
\details{
The transformation of the \emph{R} objects into a character matrix is
done through \link[base:character]{base::as.character} and using \link[base:matrix]{base::as.matrix} when the
method is defined for the object type. For \code{list}s, \code{NA} are appended to the
elements that have an object length less than the maximum object length.
}
\examples{
char_matrix(y = 1:3)
char_matrix(y = factor(x = c("a", "b", "c")), cname = "ex_name")
char_matrix(y = data.frame(a = 1:3, b = letters[1:3]))
char_matrix(y = list(one = 1:3, two = 4:6))

}
\seealso{
\link[base:matrix]{base::as.matrix} \link[data.table:data.table]{data.table::data.table} \link[tibble:tibble]{tibble::tibble}
}
