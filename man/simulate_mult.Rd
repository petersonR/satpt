% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-mult.R
\name{simulate_mult}
\alias{simulate_mult}
\title{Simulation of multinomial responses}
\usage{
simulate_mult(n, size, prob, categories = NULL)
}
\arguments{
\item{n}{Integer specifying the number of random samples to draw.}

\item{size}{Vector of integer values, say \eqn{N_i}, specifying the
sample sizes for \eqn{i = 1, \ldots , I} data collection periods.}

\item{prob}{Numeric non-negative vector or matrix specifying the
probabilities for the \emph{K} response categories in the \eqn{I} data
collection periods.}

\item{categories}{Vector of values specifying the \emph{K} category labels to
be represented. Default is \code{NULL}.}
}
\value{
A \code{data.frame} with \eqn{N = \sum_{i}N_{i}} rows and \code{n + 1}
variables, which are described below.
\describe{
\item{\code{period}}{Data collection period defined by the length of \code{size}}
\item{\verb{responses1 ... n}}{Simulated multinomial responses with either the
values defined by \code{categories} or \emph{Category k, } for
\eqn{k = 1, \ldots , K}}
}
}
\description{
Simulating the data of multinomial responses collected during a
survey. The function name is \code{simulate_mult()} (rather than \code{simulate()}) to
avoid masking \code{\link[stats:simulate]{stats::simulate()}} when the package is attached.
}
\details{
Running in the backend is \code{\link[stats:Multinom]{stats::rmultinom()}}. So, see the
documentation for \code{\link[stats:Multinom]{stats::rmultinom()}}for more information on how the
multinomial responses are generated.

The arguments \code{size} and \code{prob} are directly connect when it comes to
generating multinomial responses for multiple data collection periods. \code{prob}
may either be a vector or matrix specifying probabilities for the \emph{K}
response categories. When \code{size} is specified to generate data for multiple
data collection periods, then \code{prob} must be specified as a numeric vector of
length \code{length(size)} times \emph{K} or numeric matrix with \code{length(size)}
rows and \emph{K} columns, where each row of the matrix specify the \emph{K}
probabilities of each data collection period.

When \code{categories} is not defined (\code{categories = NULL}) \code{simulate_mult} will
create default lables of \emph{Category k, } for \eqn{k = 1, \ldots , K}.
}
\examples{
# Creating 5 simulated data sets of with a sample size of 10, where there
# are two possible response categories with a 50\% chance of being selected.
simulate_mult(n = 5, size = 10, prob = c(0.5, 0.5))

# Creating 1 simulated data set for two data collection periods, where there
# are three possible response categories that are labeled.
prob <- matrix(
  data = c(0.4, 0.4, 0.2, 0.1, 0.1, 0.8),
  nrow = 2, ncol = 3, byrow = TRUE
)
catg <- LETTERS[1:3]
simulate_mult(n = 1, size = c(20, 10), prob = prob, categories = catg)

}
\seealso{
\code{\link[stats:Multinom]{stats::rmultinom()}}
}
