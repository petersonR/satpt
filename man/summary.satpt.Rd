% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{summary.satpt}
\alias{summary.satpt}
\alias{print.summary.satpt}
\title{Summarizing the saturation point analysis}
\usage{
\method{summary}{satpt}(object, digits = max(3, getOption("digits") - 3), ...)

\method{print}{summary.satpt}(x, ...)
}
\arguments{
\item{object}{\code{satpt} object to be summarized.}

\item{digits}{Minimal number of \emph{significant digits}.
Default is \code{max(3, getOption("digits") - 3)}.}

\item{...}{Additionally, arguments passed to \code{\link[base:print]{base::print()}}.}

\item{x}{\code{summary.satpt} object to be printed.}
}
\value{
An object with \code{S3} class \code{"summary.satpt"} containing 10 elements
created by \code{\link[=summary]{summary()}}.
\describe{
\item{\code{threshold}}{Saturation threshold applied to the standard errors of
the sample proportions.}
\item{\code{saturation}}{A character value indicating whether all response
categories have achieved saturation given the defined \code{threshold}. The value
of \code{"Yes"} indicates that saturation has been achieved while a value of
\code{"No"} indicates that saturation was not achieved and more data is needed
to achieve saturation.}
\item{\code{which_saturation}}{A character value indicating which collection of
responses within \code{y} determined saturation achievement. Generally, this is
only of importance when examining select all apply questions.}
\item{\code{n}}{Total number of observations with a response provided.}
\item{\code{phat}}{A \code{matrix} containing the row-wise sample proportions for the
observed contigency table. The values are formatted by \code{digits}.}
\item{\code{se}}{A \code{matrix} containing the standard errors for the calculated
sample proportions (\code{phat}). The values are formatted by \code{digits}.}
\item{\code{pooled_se}}{A logical value indicating whether pooled standard errors
were calculated due to the presence of response bias.}
\item{\code{alpha}}{Significance level for test forindependence.}
\item{\code{test}}{A \code{htest} object produced by \code{\link[stats:chisq.test]{stats::chisq.test()}} containing
the results from the test for independence.}
\item{\code{hindex}}{Heterogeneity index for the sample proportions calculated by
mean absolute deviation. The values are formatted by \code{digits} if \code{hindex}
is not \code{NULL} in \code{object}.}
}
}
\description{
Creates a conditional summary of the saturation point analysis
performed by \code{\link[=satpt]{satpt()}}.
}
\details{
For printing \code{summary.satpt} objects, the \code{digits} parameter does
not need to be specified in \code{print} as \code{\link[=summary]{summary()}} already takes
care of the formatting of \emph{significant digits}.
}
\examples{
# Creating an example, where response bias is present.

## Simulating data
prob <- matrix(
  data = c(0.4, 0.4, 0.2, 0.1, 0.1, 0.8),
  nrow = 2, ncol = 3, byrow = TRUE
)
catg <- LETTERS[1:3]
set.seed(123)
dat <- simulate_mult(
  n = 1, size = c(200, 100), prob = prob, categories = catg
)

## Determining saturation with response bias and summarizing analysis
res <- satpt(y = dat$responses1, by = dat$period)
summary(object = res, digits = 3)

}
\seealso{
\code{\link[=satpt]{satpt()}}
}
